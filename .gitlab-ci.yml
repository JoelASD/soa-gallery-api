stages:
    - test

before-script:
    - apk add openssh

test:
    stage: test
    only:
        - master
    script:
        - ssh -i $SSH_PRIVATE_KEY ubuntu@$SSH_HOST
        - ls
    tags:
        - general
