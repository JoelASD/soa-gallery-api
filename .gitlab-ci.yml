image: ubuntu
stages:
    - test

before_script:
    - apt-get update -y && apt-get install openssh-client -y

test:
    stage: test
    only:
        - master
    script:
        - ssh -i $SSH_PRIVATE_KEY ubuntu@$SSH_HOST
        - ls
    tags:
        - general
