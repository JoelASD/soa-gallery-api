image: alpine:3.7
stages:
    - test

test:
    stage: test
    only:
        - master
    script:
        - apk add openssh
        - ssh -i $SSH_PRIVATE_KEY ubuntu@$SSH_HOST
        - ls
    tags:
        - general
