stages:
    - test

test:
    stage: test
    only:
        - master
    script:
        - apt-get install openssh-server
        - ssh -i $SSH_PRIVATE_KEY ubuntu@$SSH_HOST
        - ls
    tags:
        - general
